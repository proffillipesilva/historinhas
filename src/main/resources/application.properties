server.port=${SPRING_PORT:8080}
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/historinhas
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:1234}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
spring.task.scheduling.pool.size=10
spring.jpa.show-sql=true
spring.jpa.database=MYSQL

jwt.secret=lpiiiback

spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENTID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_SECRET}
spring.security.oauth2.client.registration.google.scope=profile, email, drive, docs

spring.cloud.azure.storage.blob.account-name=historinhas
spring.cloud.azure.storage.blob.account-key=${AZURE_STORAGE_KEY}
spring.cloud.azure.storage.blob.endpoint=${AZURE_CONTAINER_URL}

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB